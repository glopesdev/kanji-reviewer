<Page
    x:Class="KanjiReviewer.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KanjiReviewer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.BottomAppBar>
        <CommandBar>
            <CommandBar.Content>
                <Grid/>
            </CommandBar.Content>
            <AppBarButton Icon="Accept" Label="Yes"/>
            <AppBarButton Icon="Cancel" Label="No"/>
            <AppBarButton Icon="Favorite" Label="Easy"/>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid x:Name="LayoutRoot" Background="WhiteSmoke">
        <StackPanel>
            <TextBlock Text="KANJI REVIEWER" FontSize="30"/>
            <TextBlock Name="PageTitle" Text=""/>
        </StackPanel>
        
        <Border BorderBrush="Black" BorderThickness="2"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Height="300" Margin="0,0,0,0"
                Width="{Binding Height, RelativeSource={RelativeSource Self}}">
            <Canvas Background="White"/>
        </Border>
    </Grid>
</Page>
